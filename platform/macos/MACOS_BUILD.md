# macOS Build

Public macOS build instructions are maintained in `../../docs/BUILD_MACOS.md`.

The script in this directory is the macOS build/validation entry point:

```sh
platform/macos/build-macos-release.sh --arch arm64
platform/macos/build-macos-release.sh --arch x86_64
```
