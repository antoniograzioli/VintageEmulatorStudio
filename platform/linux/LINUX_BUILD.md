# Linux Build

Public Linux build instructions are maintained in `../../docs/BUILD_LINUX.md`.

The script in this directory is the Linux build/validation entry point:

```bash
platform/linux/build-linux-release.sh
```
